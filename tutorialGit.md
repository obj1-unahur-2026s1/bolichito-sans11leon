//primero crea una carpeta para clonar el repositorio de git
//click derecho y git bash
//poner git clone "URL"
//poner git config --global user.name "nombre" y git config --global user.email "mail"
//borrando las comillas y su contenido se puede ver el resultado del comando
//luego para acceder a la carpeta del repositorio despues de clonar y moduficar, en git ( cd "nombre del repositorio + Tab" )
//poner "git add ." para actualizar el repositorio local
//luego poner " git commit -m "MENSAJE" "
// y por ultimo, git push (revisar si hay credenciales de windows de github y borrar si es desconocida)